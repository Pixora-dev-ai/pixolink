{
  "name": "@pixora/pixolink-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "PixoLink Unified SDK - Modular AI, Payment, Security & Admin Platform",
  "author": "PixoRA Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/pixora/pixolink.git"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\"",
    "publish:all": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.9",
    "prettier": "^3.4.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
